# 🌍 Многоязычная настройка сайта LegalGrup

Ваш сайт теперь настроен для поддержки трех языков:
- 🇬🇧 **English (en)** - Английский
- 🇷🇺 **Русский (ru)** - Russian  
- 🇷🇴 **Română (ro)** - Румынский

## ✅ Что уже сделано

1. **Инфраструктура i18n:**
   - ✅ Создана система типов для переводов
   - ✅ Настроен React Context для доступа к переводам
   - ✅ Созданы файлы переводов для всех трех языков
   - ✅ Настроен middleware для автоматического редиректа

2. **Роутинг:**
   - ✅ Middleware перенаправляет на локализованные маршруты (`/en`, `/ru`, `/ro`)
   - ✅ Создана структура `app/[locale]/` для локализованных страниц
   - ✅ Layout с поддержкой локали

3. **Компоненты:**
   - ✅ Создан компонент переключателя языка (`LanguageSwitcher`)
   - ✅ Создан компонент локализованных ссылок (`LocalizedLink`)

## 📁 Структура файлов

```
lib/i18n/
  ├── config.ts              # Конфигурация языков
  ├── types.ts               # TypeScript типы
  ├── context.tsx            # React Context
  ├── utils.ts               # Утилиты для работы с путями
  ├── link.tsx               # Компонент локализованных ссылок
  └── translations/
      ├── en.ts              # Английские переводы
      ├── ru.ts              # Русские переводы
      └── ro.ts              # Румынские переводы

app/
  ├── [locale]/              # Локализованные страницы
  │   ├── layout.tsx
  │   └── page.tsx
  ├── page.tsx               # Редирект на /en
  └── components/
      └── LanguageSwitcher.tsx

middleware.ts                # Middleware для редиректов
```

## 🚀 Как использовать переводы

### В компонентах

```tsx
"use client";
import { useTranslations } from "../../lib/i18n/context";

export function MyComponent() {
  const { t, locale } = useTranslations();
  
  return (
    <div>
      <h1>{t.home.hero.title}</h1>
      <p>{t.home.hero.description}</p>
      <p>Текущий язык: {locale}</p>
    </div>
  );
}
```

### Локализованные ссылки

```tsx
import { LocalizedLink } from "../../lib/i18n/link";
import { useTranslations } from "../../lib/i18n/context";

export function Navigation() {
  const { t } = useTranslations();
  
  return (
    <nav>
      <LocalizedLink href="/services">
        {t.nav.services}
      </LocalizedLink>
    </nav>
  );
}
```

### Переключатель языка

```tsx
import { LanguageSwitcher } from "../components/LanguageSwitcher";

export function Header() {
  return (
    <header>
      {/* ... другой контент ... */}
      <LanguageSwitcher />
    </header>
  );
}
```

## 📝 Следующие шаги (что нужно сделать)

### 1. Мигрировать страницы в структуру `[locale]`

Все страницы нужно переместить из `app/` в `app/[locale]/`:

**Текущая структура:**
```
app/
  ├── page.tsx
  ├── services/
  │   └── page.tsx
  ├── attorneys/
  │   └── page.tsx
  ├── insights/
  │   └── page.tsx
  └── contact/
      └── page.tsx
```

**Новая структура:**
```
app/
  └── [locale]/
      ├── page.tsx
      ├── services/
      │   └── page.tsx
      ├── attorneys/
      │   └── page.tsx
      ├── insights/
      │   └── page.tsx
      └── contact/
          └── page.tsx
```

### 2. Обновить страницы для использования переводов

Для каждой страницы:

1. Переместить файл в `app/[locale]/`
2. Добавить импорт переводов:
   ```tsx
   import { useTranslations } from "../../../lib/i18n/context";
   ```
3. Заменить все текстовые строки на `t.key.path`
4. Обновить ссылки на `LocalizedLink`

**Пример миграции:**

```tsx
// БЫЛО (app/services/page.tsx):
export default function ServicesPage() {
  return (
    <div>
      <h1>Services</h1>
      <p>Our legal services...</p>
    </div>
  );
}

// СТАЛО (app/[locale]/services/page.tsx):
"use client";
import { useTranslations } from "../../../lib/i18n/context";

export default function ServicesPage() {
  const { t } = useTranslations();
  
  return (
    <div>
      <h1>{t.services.hero.title}</h1>
      <p>{t.services.hero.description}</p>
    </div>
  );
}
```

### 3. Обновить компоненты

Обновить `SiteChrome.tsx`, формы и другие компоненты для использования переводов.

### 4. Добавить переключатель языка в навигацию

Добавить `<LanguageSwitcher />` в компонент навигации.

## 🔗 URL структура

После миграции URL будут работать так:

- `/en` → Английская версия главной страницы
- `/ru` → Русская версия главной страницы
- `/ro` → Румынская версия главной страницы
- `/en/services` → Английская версия страницы услуг
- `/ru/services` → Русская версия страницы услуг
- `/` → Автоматический редирект на `/en`

## 📚 Доступные переводы

Все доступные ключи переводов определены в `lib/i18n/types.ts`. Основные разделы:

- `t.common.*` - Общие строки
- `t.nav.*` - Навигация
- `t.home.*` - Главная страница
- `t.services.*` - Услуги
- `t.attorneys.*` - Адвокаты
- `t.insights.*` - Инсайты
- `t.contact.*` - Контакты
- `t.forms.*` - Формы

## ⚠️ Важные замечания

1. **Все страницы должны быть client components** если используют `useTranslations()` (добавьте `"use client"` в начало файла)

2. **Импорты должны быть обновлены** при перемещении файлов (относительные пути изменятся)

3. **Ссылки должны использовать `LocalizedLink`** для сохранения текущей локали

4. **Переводы добавляются во все три файла** (`en.ts`, `ru.ts`, `ro.ts`)

## 🛠️ Добавление новых переводов

1. Откройте `lib/i18n/types.ts` и добавьте новый ключ в интерфейс `TranslationKeys`
2. Добавьте переводы в:
   - `lib/i18n/translations/en.ts`
   - `lib/i18n/translations/ru.ts`
   - `lib/i18n/translations/ro.ts`
3. Используйте новый ключ в компонентах: `t.new.key.path`

## 📞 Поддержка

Если возникнут вопросы по миграции, обращайтесь за помощью. Основная инфраструктура уже настроена, осталось только мигрировать существующие страницы и компоненты.

